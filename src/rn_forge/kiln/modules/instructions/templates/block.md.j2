
<!-- Generated by kiln {{ kiln_version }} from .rn-forge/kiln/config.toml — do not edit; run 'kiln apply' -->

## Repository standard (kiln)

This repo is generated and checked by [kiln](https://github.com/rn-forge/kiln).
The standard, rendered for this repo's archetype and docs profile, is
[.rn-forge/kiln/standard.md](.rn-forge/kiln/standard.md). Read it before
changing anything structural.

- **Archetype:** `{{ display_archetype }}` · **Docs profile:** `{{ docs_profile }}` · **CI:** GitHub
  Actions, Sonar {% if sonar %}on{% else %}off{% endif %}
{% if docs_profile == "mkdocs" %}- **Entrypoint:** `task`. Never invoke `uv`, `pytest`, `ruff`, `pyright`,
  `mkdocs` or `kiln` directly, in a workflow or in a document — the ten root
  verbs are
  `setup validate lint format typecheck test test:coverage build clean version`,
  plus `docs:build docs:serve docs:nav docs:structure`.
{% else %}- **Entrypoint:** `task`. Never invoke `uv`, `pytest`, `ruff`, `pyright` or
  `kiln` directly, in a workflow or in a document — the ten root verbs are
  `setup validate lint format typecheck test test:coverage build clean version`.
{% endif %}- **Generated files** carry a provenance header naming kiln. They are owned by
  kiln, not by this repo: edit `.rn-forge/kiln/config.toml` and run
  `kiln apply`. An edit in place is drift, and `task lint` fails on it.
{% if docs_profile == "mkdocs" %}- **Docs rules:** [docs/\_structure.md](docs/_structure.md) says what belongs
  where; [docs/index.md](docs/index.md) is the reader's entry point. Run
  `task docs:nav` after adding a page.
{% elif docs_profile == "external" %}- **Docs:** external, at <{{ external_url }}>.
{% endif %}- **kiln is a pinned dev dependency.** CI runs it read-only through
  `task validate` — `kiln doctor` on every run, `kiln doctor --full` where the
  workflow asks — and never runs `kiln apply`.

