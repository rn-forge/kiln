
<!-- Generated by kiln {{ kiln_version }} from .rn-forge/kiln/config.toml — do not edit; run 'kiln apply' -->

## Repository standard (kiln)

This repo is generated and checked by [kiln](https://github.com/rn-forge/kiln).
The standard, rendered for this repo's archetype and docs profile, is
[.rn-forge/kiln/standard.md](.rn-forge/kiln/standard.md). Read it before
changing anything structural.

{% if web_app %}- **Archetype:** `{{ display_archetype }}` (backend: `{{ backend }}`, frontend: `{{ frontend }}`) ·
  **Docs profile:** `{{ docs_profile }}` · **CI:** GitHub Actions, Sonar {% if sonar %}on{% else %}off{% endif %}
{% elif web %}- **Archetype:** `{{ display_archetype }}` (backend: `{{ backend }}`) · **Docs profile:**
  `{{ docs_profile }}` · **CI:** GitHub Actions, Sonar {% if sonar %}on{% else %}off{% endif %}
{% else %}- **Archetype:** `{{ display_archetype }}` · **Docs profile:** `{{ docs_profile }}` · **CI:** GitHub
  Actions, Sonar {% if sonar %}on{% else %}off{% endif %}
{% endif %}{% if docs_profile == "mkdocs" %}- **Entrypoint:** `task`. Never invoke `uv`, `pytest`, `ruff`, `pyright`,
  `mkdocs`{% if web_app %}, `pnpm`, `npx`, `nx`, `ng`{% endif %} or `kiln` directly, in a workflow or in a document — the ten root
  verbs are
  `setup validate lint format typecheck test test:coverage build clean version`,
  plus `docs:build docs:serve docs:nav docs:structure`{% if web %} and `api:dev`{% if backend == "django" %} `api:migrate`{% endif %}{% endif %}{% if web_app %}, plus `web:lint web:test web:build web:dev`{% endif %}.
{% else %}- **Entrypoint:** `task`. Never invoke `uv`, `pytest`, `ruff`, `pyright`{% if web_app %}, `pnpm`, `npx`, `nx`, `ng`{% endif %} or
  `kiln` directly, in a workflow or in a document — the ten root verbs are
  `setup validate lint format typecheck test test:coverage build clean version`{% if web %}, plus `api:dev`{% if backend == "django" %} `api:migrate`{% endif %}{% endif %}{% if web_app %}, plus `web:lint web:test web:build web:dev`{% endif %}.
{% endif %}{% if web_app %}- **Frontend:** `{{ frontend }}`, managed by pnpm + Nx at `{{ web_dir }}` (project
  `{{ web_project }}`). Every file the frontend scaffold wrote is repo-owned;
  kiln renders nothing under `{{ web_dir }}`.
{% endif %}- **Generated files** carry a provenance header naming kiln. They are owned by
  kiln, not by this repo: edit `.rn-forge/kiln/config.toml` and run
  `kiln apply`. An edit in place is drift, and `task lint` fails on it.
{% if docs_profile == "mkdocs" %}- **Docs rules:** [docs/\_structure.md](docs/_structure.md) says what belongs
  where; [docs/index.md](docs/index.md) is the reader's entry point. Run
  `task docs:nav` after adding a page.
{% elif docs_profile == "external" %}- **Docs:** external, at <{{ external_url }}>.
{% endif %}- **kiln is a pinned dev dependency.** CI runs it read-only through
  `task validate` — `kiln doctor` on every run, `kiln doctor --full` where the
  workflow asks — and never runs `kiln apply`.

