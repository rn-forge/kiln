# ADR-0001 — Product code in a golden repo stays trivial

**Status:** accepted

## Context

A golden repo is the byte-exact oracle for kiln's templates (kiln ADR-0005):
`render(golden config) == golden bytes`. It is also a *runnable* repo —
`uv sync` and `task validate` pass in it standalone — because a template that
only type-checks in the abstract has never been proven to produce a working
repo.

Those two jobs pull in opposite directions the moment the product grows. Real
product code brings dependencies, which bring lockfile churn, which makes the
fixture expensive to keep green; and it invites reviewers to read the product
instead of the scaffolding, which is the one thing they are here to read.

## Decision

The product is one function, one command, a three-line `main`, and their tests.
Every other file in the repo is either generated by kiln, a block kiln owns, or
something kiln seeded once.

The command line is not part of that budget, because it is not written: the
`[cli]` table in `.rn-forge/kiln/config.toml` declares it and
`rn_forge.cli.CliApp.from_config` builds it (kiln ADR-0009). A repo that had to
hand-write app construction to have a CLI would have had to put real code here.

A reviewer reading this repo top to bottom is reading the standard, not an
application.

## Consequences

- `task validate` proves the scaffolding end to end and nothing else. That is
  the intent: the archetype's job is the scaffolding.
- Anything that needs a real application to exercise it — a framework generator,
  a web frontend — is a different archetype's fixture, not extra code here.
- The dependency set stays small enough that `uv sync` in CI is cheap, so the
  snapshot test can afford to run the whole gate.
