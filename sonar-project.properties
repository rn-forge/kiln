# Generated by kiln golden from .rn-forge/kiln/config.toml — do not edit; run 'kiln apply'
sonar.projectKey=rn-forge_kiln
sonar.projectName=kiln
sonar.organization=rn-forge

sonar.sources=src
sonar.tests=tests

sonar.python.version=3.14
sonar.python.coverage.reportPaths=coverage.xml

# The golden repos are complete repositories checked out inside this one. They
# are fixtures, held to their own archetype's configuration by their own
# `task validate`, and their deliberate byte-for-byte duplication of each
# other's generated files is the property under test, not a finding.
sonar.exclusions=tests/fixtures/**
